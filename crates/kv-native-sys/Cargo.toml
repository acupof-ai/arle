[package]
name = "kv-native-sys"
version = "0.2.1"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
libc = "0.2"
memmap2 = { workspace = true }
nix = { version = "0.31", features = ["fs", "mman"] }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
