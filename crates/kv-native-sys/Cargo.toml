[package]
name = "kv-native-sys"
version = "0.3.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
anyhow = "1.0"
infer-seam = { path = "../infer-seam" }
libc = "0.2"
log = "0.4"
memmap2 = { workspace = true }
nix = { version = "0.31", features = ["fs", "mman"] }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
