[package]
name = "kv-native-sys"
version = "0.5.3"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
anyhow = "1.0"
infer-seam = { path = "../infer-seam" }
libc = "0.2"
log = "0.4"
memmap2 = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
io-uring = "0.7"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
