# Generated TileLang AOT artifacts — NOT committed. The build regenerates per-SM
# cubins on demand via TileLang (nvcc cross-compiles every SM incl. Blackwell, no
# matching GPU needed) into this dir + a hash-keyed cache. Distribution of a
# prebuilt pack for TileLang-less builds is a separate (public) channel (TODO).
generated/
