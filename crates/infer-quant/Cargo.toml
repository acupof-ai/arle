[package]
name = "infer-quant"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

# Host-only weight-quant checkpoint ABI detection and numeric codecs, moved
# out of infer-cuda verbatim: zero device calls, verifiable on a Mac.

[dependencies]
anyhow = "1.0"
safetensors.workspace = true
serde.workspace = true
serde_json.workspace = true
half = "2.4"

[lints]
workspace = true
