{"name": "spec-parity-gate-validation", "started": "2026-09-09T08:35:46Z", "cmd": "scripts/spec_parity.py (negative control then green run), Qwen3.5-0.8B + r3lax DSpark draft, Metal", "hypothesis": "negative control (draft arm temp 0.3) reports >=1 mismatch and exits 1; green run reports zero mismatches, accept rate > 0, both arms print decode tok/s", "status": "ok", "ended": "2026-09-09T09:38:43Z", "result": "negative control: 4/4 mismatches, exit 1 (red confirmed). green run: 7/8 mismatches at tokens 13-50, exit 1 (PARITY FAILED). accept_rate 0.655 derived, baseline 294.6 tok/s vs draft 100.5.", "finding": "The gate works in both directions: it detects the injected negative-control divergence and caught a real correctness bug in the Metal DSpark verify path. The default pairing (Qwen3.5-0.8B + DSpark) is not correctness-preserving. Both Markov and DSPARK_NO_MARKOV=1 draft paths diverge identically (7/8, tokens 13-50), ruling out the draft head — every emitted token is a target argmax. The drift accumulates over steps (absent at token 0), consistent with GDR recurrent-state drift in the hybrid target's batched verify forward vs the per-token decode forward.", "decision": "The gate is the admission test for Metal draft pairings: spec_parity.py must pass zero-mismatch before a pairing is used in a benchmark or default. Runtime fix (GDR recurrence numerics or post-rollback recompute) is a separate work item. Errors entry: docs/experience/errors/2026-09-09-dspark-draft-metal-parity-drift.md", "entry": "docs/experience/errors/2026-09-09-dspark-draft-metal-parity-drift.md"}
{"name": "doctor-solve-tolerance-35b", "started": "2026-09-09T08:35:46Z", "cmd": "arle --doctor --model-path mlx-community/Qwen3.6-35B-A3B-4bit; arle serve same model; measure phys_footprint", "hypothesis": "printed weights+headroom+reserve+KV solve matches measured resident bytes within a tolerance small enough to make the solve an audit tool", "status": "ok", "ended": "2026-09-09T09:38:48Z", "result": "35B: rejected by resource guard (fixed 38 GiB > 34 GiB max limit under swap pressure), no resident to measure. 0.8B fallback: printed fixed+KV sum 12 GiB vs measured phys_footprint 1.2 GiB (plain) / 1.5 GiB (draft).", "finding": "The printed fixed-plus-KV sum is a capacity ceiling, not a steady-state resident prediction. The ~10 GiB gap on the 0.8B is MLX allocation laziness: the KV pool (2.7 GiB) and GDR static state (4,770 MiB, one recurrent state per slot) materialize on first touch, and a short parity workload touches a fraction of one slot. The solve is an audit tool for the fixed set and the memory limit, not for resident bytes. The 35B resident measurement is blocked by swap pressure on this box (13 GiB swap used).", "decision": "Note 4 documents the tolerance as: the sum overestimates resident by the lazily-allocated KV/GDR capacity; resident approaches the sum as the workload fills the KV pool. 35B resident measurement deferred to a box without swap pressure (same deferral as note 1). Wins entry: docs/experience/wins/2026-09-09-doctor-prints-resource-solve.md", "entry": "docs/experience/wins/2026-09-09-doctor-prints-resource-solve.md"}
{"name": "kv-tier-cold-page-read", "started": "2026-09-09T09:11:58Z", "cmd": "crates/kv-native-sys/examples/cold_page_read.rs --disk /mnt/data02 --pages 64", "hypothesis": "A cold KvTierStore page fault (read + consume 21.77 MiB) fits one DSv4-Flash decode step (15.25 ms) on L2 host DRAM but not on L3 NVMe", "status": "ok", "ended": "2026-09-09T09:11:58Z", "result": "L2 fault p50 2.31 ms (15% of budget); L3 fault p50 20.85 ms (137%)", "finding": "L2 read path is a 1us BTreeMap borrow; the fault cost is the host-side copy (18.9 GB/s). L3 read is IO-dominated (19.2 ms, 1.13 GB/s O_DIRECT) and exceeds the budget on its own.", "decision": "Per-page paging of active sequences from L3 killed; roadmap Goal 2 item 2 deleted. L2 path remains viable.", "entry": ""}
{"name": "mtp-batch-shape-check", "started": "2026-09-09T09:25:51Z", "cmd": "serve tc27-nvfp4-slice2 --spec-type auto --mtp-draft-tokens 2 --kv-cache-dtype fp8 --spec-max-batch 1 vs 16; c=8 staggered 32K-trace load; read /v1/stats mixed-step share", "hypothesis": "batched MTP verify does not hit the mixed-step scheduler stall that killed batched DSpark on quantized KV (errors/2026-08-22): mixed-step count and busy share stay within the per-row envelope", "status": "running"}
