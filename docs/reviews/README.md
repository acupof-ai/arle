# docs/reviews — historical archive

Review documents are dated snapshots, never resynced. Some predate the
2026-06-04 rewrite (`e81b98fb`) and reference the deleted monolithic `infer/`
crate; read those paths as historical. The kernel six-principles review keeps
its standing as the GPU heat-map reference (see root `AGENTS.md` §GPU kernel
work) even though its file paths are pre-rewrite.
