# docs/research — historical archive

Research notes are dated evidence snapshots, never resynced. Many predate the
2026-06-04 rewrite (`e81b98fb`) and reference the deleted monolithic `infer/`
crate; read those paths as historical. Current truth lives in
[`../index.md`](../index.md) and the code itself.
