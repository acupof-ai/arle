# Roadmap — 2026-08-24

Forward-ordered next actions only. Evidence lives in CHANGELOG, `docs/experience/`, and sibling plan docs — this file ranks, it does not record. When an item ships or is killed, delete its line here; the verdict stays in its experience entry.

## Standing goals (ranking function)

1. **27B inference = global SOTA, every feature model-universal** (`project_goal_27b_sota_and_universal_features`). Our own claims use TTFT/ITL separately, never out-tok/s; the public comparison point is Qwen3-32B FP8 on H20+SGLang.
2. **L2/L3 = lossless KV capacity** (`project_goal_kv_capacity_via_l2_l3`).
3. **OPD = the differentiator**, SOPD DSv4-Flash teacher → 35B student (`project_opd_sopd_target_flash_teacher_35b_student`).
4. Observability — shipped 2026-08-17; only its remote tail remains (see triage).

## Goal 0 — Distribution (added 2026-09-02)

The front door is the coding-agent server (README, landing). Gate for a public
post: one clean number on the canonical model.

1. **Qwen3.6-35B-A3B per-turn TTFT row.** `scripts/bench_multiturn_ttft.py --turns 12 --warmup`, arle vs `mlx_lm.server --prompt-cache-size 4`, on a Mac without swap pressure; replace the 0.8B row in README / README.zh-CN / benchmarks. The 2026-09-02 attempt measured swap (decode 16 vs 85 tok/s).
2. **Metal resource guard on a lived-in 48 GB Mac.** The canonical model was rejected at a 23 GiB fixed requirement (19 GiB weights + 4 GiB headroom) with 21.9 GiB available and 17 GB of swap from other apps; it served only with `ARLE_METAL_AVAILABLE_RESERVE_MB=512 ARLE_METAL_RUNTIME_HEADROOM_MB=768 --allow-swap --memory-budget-bytes 22GiB --mem-fraction-static 0.97`. Decide the desktop default or print that recipe in the rejection.
3. **Post** to r/LocalLLaMA and HN with the 35B row and the Claude Code line, once (1) lands.

## Goal 1 — 27B SOTA

1. **Batched MTP verify.** The measured top NVFP4 lever: inert from c=4 until the verify step is batched (CHANGELOG 2026-08-22); projected −32% at c=16. Plan: `2026-08-21-batched-mtp-verify.md`. `--spec-type auto` is already the default.
2. **DSpark draft acceptance collapse on Qwen3.8-27B-DSpark** (13% c=1, 0% c=8). Untested candidate: mRoPE — target uses `partial_rotary_factor: 0.25`, drafter runs full standard RoPE. `errors/2026-08-23-dspark-rope-fix-was-not-the-cause.md`.
3. **DSv4 prefill i32 work-size overflow >~1560 tokens** — breaks prefill on the SOTA family at shapes every real prompt exceeds. `architecture.md:263`.
4. **NVFP4 c=1 gap vs FP8** (44.4 vs 59.5 tok/s, DSv4 precision matrix 2026-08-24): structural to the compute mix, not GEMV restructuring (see Killed). Un-examined axis: `gdr_decode_batch_kernel` (13% of decode kernel time). Backlog: `2026-08-21-nvfp4-decode-lever-backlog.md`.
5. **CP ideal-state T3.4/T4.** T3.1 (B2 CP decode head-sharding), T3.2/T3.2b (2D KV ownership), T3.3 (cross-CP decode merge) shipped; 262,144 global-seq training on 2 GPUs accepted 2026-08-22. Plan: `2026-08-16-cp-ideal-state.md`. Open tail: single-GPU 256K training acceptance (`2026-07-29-single-gpu-256k-training-repair.md`, T6: CP is not a substitute); long-ctx multi-shape perf under #207/#226.
6. **DSv4 FP8/FP4 block-scaled weight path** — kill-or-keep decision unrecorded; likely subsumed by NVFP4→W4AFP8. `docs/quantization.md` §2.

Enabling, no perf campaign: Metal feature parity (LFM2.5-8B-A1B, DSpark block_size=4 shipped 2026-08-23; decode directions ruled out 2026-08-21); weight-quant dispatch consolidation tranches 1–2A shipped, residual = MoE dispatch + global storage rewrite (`2026-08-20-qwen-quant-linear-dispatch-consolidation.md`).

## Goal 2 — L2/L3 lossless KV capacity

1. **Measure `KvTierStore` cold page-read latency** (microbench, no GPU-contention dependency). The design risk is a decode step stalling on a cold page; the park-vs-recompute A/B prices preemption economics and cannot bound it. Constituent numbers already held: 26.6 s cold restore of a 3,375-token slot (`wins/2026-07-02-kv-unified-tier-flags-pod-verify`), 125–130 ms promote per 10,130 tokens (`wins/2026-08-19-cp-slot-park-works-l2-l3-nonzero`).
2. **Per-page paging of active sequences** — fault-around-attention attention contract + recency policy; gated on (1).
3. **Compressor BF16 full-history retention ring** (5.5 GB/slot at 1M tokens) — prefill-numerics change behind the needle gate.

## Goal 3 — OPD

Lane order: math-opd → agent-OPD measurement integrity → SOPD scale-up.

1. **GSPO math run 2 verdict** — reward fix `39c43d5d2`; watch signal: eval median length below 8192 by round 6. `errors/2026-08-24-gspo-math-opd-zero-length-gradient.md`.
2. **Agent-OPD eval confinement** — answer-key leak fixed 2026-08-24 (`errors/2026-08-24-rollout-agent-read-its-own-answer-key.md`); `opd_corpus_preflight.py` is the pre-flight. Baselines before this date stand only where unscored.
3. **NVFP4 tool-call corruption** (ThinkingCap-Qwen3.6-27B-NVFP4; Qwen3.8-27B-NVFP4 is clean through identical code — proceed on Qwen3.8 while debugging). `errors/2026-08-23-nvfp4-tool-calls-corrupt.md`.
4. **SOPD scale-up**: InferTeacher D2D bridge → 35B student, teacher time-shared via `offload_engine_weights`.
5. **bf16 tape Stage 3** (CE/logits-grad) — Stages 1–2 landed; flag inert on the checkpointed writeback path. `2026-07-25-fullchain-bf16-tape.md`.

## Killed — do not relitigate

- lm_head vocab-parallel shard (#99) — implemented, never licensed, deleted `9b12060fc` (2026-08-13).
- DP-attn for throughput (#89) — wash at B=1, closed 2026-08-17; long-ctx lives under CP (#207/#226).
- DSpark trigger routing / import-not-train (#127/#128) — closed; DSpark is the SOTA DSv4-Flash-FP8 serve config (baselines re-anchor 2026-08-14, c=1 65.56 tok/s, accept 50.36%).
- DSpark ISO paper-faithful head — premise falsified 2026-07-28; ISO stays non-default.
- Batched DSpark on quantized KV — rejected ×2; mixed-step scheduler stalls, parity holds. Ships per-row at c=1.
- `--kv-recall` — quality trade, uniformly slower, deleted 2026-08-19.
- W4AFP8 GEMV restructuring — memory-bound at M=1, custom kernel perf-neutral (`wins/2026-08-22-w4afp8-custom-decode-kernel`).
- M-split GEMV grid — ruled out 2026-08-23 (+2–6%).
- Metal decode three directions — ruled out 2026-08-21.

## Pending-remote triage (H20 window; re-check staleness before running)

GLM-5.2 forward verification (oldest, 2026-06-19) · canonical TP runtime L4 gate (2026-07-22) · operator evidence ×15, contract fixed 2026-08-20 · tier budget ÷ world (2026-08-17) · W4AFP8 prefill m≥64 (2026-08-20) · 2D CP empty-shard needle ladder (2026-08-18) · replicated radix CP (2026-08-17) · finite-step negative case (2026-07-27) · full-run ledger (2026-08-10) · quant-linear storage validation (2026-08-20) · agent-OPD fleet cp=2 A/B (2026-08-07) · cross-rank stats e2e (2026-08-19) · observability multiproc TP (2026-08-17) · DSA rotated-key transient ring needle gate (`architecture-dsv4.md:301`) · Qwen3.5-122B-A10B TP4 completion gate.
