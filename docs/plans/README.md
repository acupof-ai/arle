# docs/plans — historical archive

Plan documents are **archive-only, not authority** — they capture the design
state at the time of writing and are never resynced. Many predate the
2026-06-04 rewrite (`e81b98fb`) and reference the deleted monolithic `infer/`
crate (`infer/src/**`, `-p infer`, `metal_serve`, …); read those paths as
historical. Current truth: [`../index.md`](../index.md),
[`../architecture.md`](../architecture.md),
[`../codebase-map.md`](../codebase-map.md).
