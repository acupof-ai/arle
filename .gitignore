__pycache__/
*.pyc
*.egg-info/
dist/
build/
*.egg
.eggs/
/target/
**/target/
/target-off/
.venv/
.claude/.DS_Store
.claude/**/.DS_Store
.claude/scheduled_tasks.lock
.claude/worktrees/
release/
.gstack/
models/
.DS_Store
._*
.context/
# Build-time-fetched official kernels stay untracked; the llama.cpp
# reference-kernel copy (pinned, see its README) is committed.
/vendor/*
!/vendor/llama.cpp/
bench-output/
# `scripts/bench_throughput.py` writes these into the cwd, not bench-output/,
# so a run from the repo root leaves them in git status. Real numbers belong in
# docs/baselines.md; the raw dump is scratch.
/bench_throughput.csv
/bench_throughput.json
opd-output/
runs/
observe-data/

# web frontend build
web/node_modules/
web/dist/
web/.astro/

# repo-local toolchains (legacy slot — kept ignored even though no toolchain
# currently lands here, so a stray repo-local install never trips git status).
/.toolchains/

# codex CLI per-session artifact (codex review --uncommitted writes a 0-byte .codex)
/.codex
traces/
.nsys-tmp/

# micro-experiment scratch (gitignored license-or-kill spike workdir)
/_gap_a_micro_workdir/
_gap_a_micro_workdir/
scripts/_pod_*
.kami-lark/*
logs/
# gate/serve scripts drop *.log at repo root (needle_gate_identity, serve_identity, …)
/*.log
# leaked absolute-path snapshot some tool writes relative-to-root
Users/
data/opd-corpora/
doc-fetch-resources/Qb9mdxvQaoJbN6xP3TGckwSBnFe/
.arle-runs/
# understand-anything knowledge graph (local exploration artifact, regenerable)
.understand-anything/
doc-fetch-resources/
arle-kernels-*.tar.gz*

# training/eval runs write token dumps to cwd; when cwd is the source tree they
# must not move the source digest (build receipt guard)
/dumps/

# OPD training metrics log, written to cwd (same hazard as /dumps/)
/metrics.jsonl
